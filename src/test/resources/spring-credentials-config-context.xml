<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
    http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.2.xsd
	http://www.springframework.org/schema/aop
	http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
	">

    <aop:aspectj-autoproxy />
    <context:annotation-config/>

    <!--Iot api tools to leverage Bobcat and other apis-->
    <bean id="playerController" class="com.spribe.player.controller.PlayerController">
        <constructor-arg value="${com.spribe.player.controller.api.url}"/>
    </bean>

    <bean id="usersManager" class="com.spribe.player.controller.managers.UsersManager"/>

    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="targetObject" ref="usersManager"/>
        <property name="targetMethod" value="createInstance"/>
        <property name="arguments">
            <list>
                <value>${com.spribe.player.controller.supervisor.login}</value>
                <value>${com.spribe.player.controller.supervisor.role}</value>
            </list>
        </property>
    </bean>

    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="targetObject" ref="usersManager"/>
        <property name="targetMethod" value="createInstance"/>
        <property name="arguments">
            <list>
                <value>${com.spribe.player.controller.admin.login}</value>
                <value>${com.spribe.player.controller.admin.role}</value>
            </list>
        </property>
    </bean>

    <!-- ====================================================== -->
    <!-- import different variables according to active profile -->
    <!-- ====================================================== -->
    <beans profile="dev">
        <context:property-placeholder
                ignore-resource-not-found="true"
                location="classpath*:META-INF/dev.properties"/>
    </beans>

</beans>